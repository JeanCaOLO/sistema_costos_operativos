// Re-exports for backward compatibility — data now lives in Supabase
export { TIPO_COLORS, MONEDAS, formatMoneda } from '../types/areas';
export type { TipoArea, Area, Moneda } from '../types/areas';
